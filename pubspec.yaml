name: sourcebase
description: "SourceBase Flutter Client"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  file_picker: ^10.3.3
  http: ^1.6.0
  provider: ^6.1.2
  shared_preferences: ^2.5.3
  shimmer: ^3.0.0
  intl: ^0.20.2
  pdf: ^3.11.1
  printing: ^5.13.3
  url_launcher: ^6.3.0
  in_app_purchase: ^3.2.0
  in_app_purchase_android: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
